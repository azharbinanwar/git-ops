# Changelog

## 0.1.0 — 2026-07-29

- Initial split from claude-ops: `/commit-msg`, `/commit-only`, `/commit-and-push`, `/create-release`, `/create-pr`, `/pr-desc`, `/issue` moved here as-is, unchanged
